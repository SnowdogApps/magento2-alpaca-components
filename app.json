{
    "name": "magento2-alpaca-components",
    "scripts": {
        "postdeploy": "npm install -g gulp-cli && gulp fractal:build"
    },
    "buildpacks": [
      {
        "url": "https://github.com/heroku/heroku-buildpack-static.git"
      }
    ]
  }
