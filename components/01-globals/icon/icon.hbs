<svg class="icon {{ class }}"
     {{#if title}}
         title="{{ title }}"
     {{else}}
         title="{{ iconId }}"
     {{/if}}
     {{{ attributes }}}
>
    <use xlink:href="{{ svg iconId }}"></use>
</svg>
