<select class="select" name="select" id="{{ someId}}">
    {{#each options}}
        <option value="{{ value }}" {{ attributes }}>
            {{text}}
        </option>
    {{/each}}
</select>

<script src="{{ static 'select.js' }}"></script>
