<div class="radio">
    <input type="radio" id="{{ id }}" name="{{ name }}" class="radio__field">
    <label for="{{ id }}" class="radio__label">
        <span class="radio__icon"></span>
        {{ label }}
    </label>
</div>
