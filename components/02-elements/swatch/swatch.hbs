<div class="swatch {{ class }}">
    {{#if heading}}
        <{{{ heading.tag }}} class="swatch__title {{ heading.class }}">
            {{ heading.text }}
        </{{{ heading.tag }}}>
    {{/if}}

    {{#if wrapper}}
        <div class="swatch__wrapper" {{{ wrapper.attributes }}}>
    {{/if}}
        {{#each options }}
            <div
                class="swatch__option-container {{ this.class }}"
            >
                <div
                    class="swatch__option {{ this.optionClass}}"
                    id="{{this.id}}"
                    tabindex="0"
                    {{{ this.attributes }}}
                    {{#if this.color }}
                        aria-label="{{ this.color }}"
                    {{/if}}
                >
                    {{ this.text }}
                </div>
            </div>
        {{/each }}
    {{#if wrapper}}
        </div>
    {{/if}}
</div>
