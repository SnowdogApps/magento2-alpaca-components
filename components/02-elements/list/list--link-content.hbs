<{{{ listTag }}} class="list {{ class }}">
    {{# each elements }}
        <{{{ ../elementTag }}} class="list__item {{ this.class }}">
            <a href="{{ link }}" class="link {{ linkClass }}">
                {{ render (component content) contentContext }}
            </a>
        </{{{ ../elementTag }}}>
    {{/ each }}
</{{{ listTag }}}>
