<div class="page-wrapper">
    {{ render '@header' }}

    <main class="container">
        {{ render '@breadcrumbs' }}

        <ul class="catalog-grid">
            {{ render '@catalog-grid-item' product }}
            {{ render '@catalog-grid-item' product }}
            {{ render '@catalog-grid-item' product }}
            {{ render '@catalog-grid-item' product }}
            {{ render '@catalog-grid-item' product }}
            {{ render '@catalog-grid-item' product }}
            {{ render '@catalog-grid-item' product }}
            {{ render '@catalog-grid-item' product }}
            {{ render '@catalog-grid-item' product }}
        </ul>

        {{ render '@pager' }}
    </main>
</div>

{{ render '@footer' }}
