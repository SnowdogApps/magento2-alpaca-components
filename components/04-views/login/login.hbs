<div class="page-wrapper">
    {{ render '@header' }}

    <div class="container">
        {{ render '@heading' mainHeading }}
        <div class="row login">
            <div class="col-xs-12 col-md-6">
                {{ render '@heading' formHeading }}
                {{ render '@divider' }}
                <p class="login__info">
                    {{{ signInInfo }}}
                </p>
                <form class="login__form">
                    <fieldset
                        class="fieldset login__fieldset {{ signInSection.fieldset.class }}"
                        {{{ signInSection.fieldset.attributes }}}
                    >
                        <legend class="fieldset__legends login__legend {{ signInSection.legend.class }}"
                            {{{ signInSection.legend.attributes }}}
                        >
                            {{ signInSection.legend.text }}
                        </legend>
                        {{ render '@input' email }}
                        {{ render '@input' password }}
                    </fieldset>
                    <div class="login__actions">
                        {{ render '@button' loginButton }}
                        <a
                            href="{{ forgetPassword.href }}"
                            class="login__forgot-password"
                        >
                            {{ forgetPassword.text }}
                        </a>
                    </div>
                    <div class="login__validation-tips">
                        {{{ validationTips }}}
                    </div>
                </form>
            </div>
            <div class="col-xs-12 col-md-6">
                {{ render '@heading' linkHeading }}
                {{ render '@divider' }}
                <p class="login__info">
                    {{ createAccountInfo }}
                </p>
                {{ render '@button' createAccountButton }}
            </div>
            <div class="col-xs-12">
                <div class="login__company-wrapper"></div>
                    {{ render '@heading' linkCompanyHeading }}
                    {{ render '@divider' }}
                    <p class="login__info">
                        {{ createCompanyAccountInfo }}
                    </p>
                    {{ render '@button' createCompanyAccountButton }}
                </div>
            </div>
        </div>
    </div>
</div>

{{ render '@footer' }}
