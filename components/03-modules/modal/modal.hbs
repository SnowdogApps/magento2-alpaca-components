{{#if trigger }}
    {{ render '@modal-trigger' }}
{{/if}}
<div class="modal" data-modal="{{ id }}">
    <dialog class="modal__content {{ class }}">
        Test
        {{ render (component content) contentContext }}
        {{#if buttonClose }}
            {{ render '@button--icon' buttonClose }}
        {{/if}}
    </dialog>
</div>

<button class="button modal-trigger" type="button" data-popuptrigger="modal-2">test</button>

<div class="modal" data-modal="modal-2">
    <dialog  class="modal__content {{ class }}">
        Test222
        {{ render (component content) contentContext }}
        {{#if buttonClose }}
            {{ render '@button--icon' buttonClose }}
        {{/if}}
    </dialog>
</div>


<script src="{{static 'modal.js' }}"></script>
