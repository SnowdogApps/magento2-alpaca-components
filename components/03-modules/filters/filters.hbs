<{{{ tag }}} class="filters {{ class }}" {{ attributes }}>
    <div class="filters__container">
        <div class="filters__lists">
            {{> '@range' range }}
        </div>

    </div>
</{{{ tag }}}>
    <div class="filters__checkbox-list">
    {{# each checkboxes }}
        {{> '@checkbox--fake' this }}
    {{/each}}
    </div>
    {{> '@swatch' swatch}}
    {{> '@swatch--color' color }}
<script src="{{static '../filters/filters.js'}}"></script>
