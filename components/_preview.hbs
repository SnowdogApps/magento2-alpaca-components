<html>
    <head>
        <meta charset="utf-8">
        <title>{{ title }}</title>
        <link media="all" rel="stylesheet" href="{{ path '/css/styles.css' }}">
        {{> @fonts }}
        {{> @libs }}
    </head>
    <body>
        {{{ yield }}}
    </body>
</html>
